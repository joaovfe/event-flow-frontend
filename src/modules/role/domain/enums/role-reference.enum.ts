export enum ERoleReference {
  MASTER = 'MASTER',
  ADMIN = 'ADMIN',
  USER = 'USER',
}

export enum ERoleReferenceTranslate {
  MASTER = 'Master',
  ADMIN = 'Administrador',
  USER = 'Usuário',
}
