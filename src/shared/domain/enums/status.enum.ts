export enum EStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum EStatusTranslate {
  ACTIVE = 'Ativo',
  INACTIVE = 'Inativo',
}

export default { EStatus, EStatusTranslate };
