export enum ETransmission {
  MANUAL = 'MANUAL',
  AUTOMATIC = 'AUTOMATIC',
  CVT = 'CVT',
  SEMI_AUTOMATIC = 'SEMI_AUTOMATIC',
}

export enum ETransmissionTranslate {
  MANUAL = 'Manual',
  AUTOMATIC = 'Automático',
  CVT = 'CVT',
  SEMI_AUTOMATIC = 'Semi-automático',
}
